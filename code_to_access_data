import numpy as np

# Add '\input_data.npz' at the end of the path
npz = np.load(r'paste the path returned from the api call')

X_train = npz['X_train']
Y_train = npz['Y_train']
del npz

print('We have {} examples to work with'.format(Y_train.shape[0]))
